{
  "port": "SERVICE_PORT",

  "database": "DATABASE",

  "messageBroker": {
    "server": "MESSAGE_BROKER_SERVER",
    "exchangeName": "EXCHANGE_NAME"
  },

  "testDatabase": "TEST_DATABASE",

  "testMessageBroker": {
    "server": "TEST_MESSAGE_BROKER_SERVER",
    "exchangeName": "TEST_EXCHANGE_NAME"
  },

  "smtpTransport": {
    "from": "SMTP_FROM",
    "host": "SMTP_HOST",
    "port": "SMTP_PORT",
    "auth": {
      "user": "SMTP_USER",
      "pass": "SMTP_PASS"
    }
  },

  "logFile": "LOG_FILE"
}
